// const contentModel = require("./content.model");

const UserModel = require("./users.model");

module.exports = () => {
  // MediaModel.hasOne(UserModel, {
  //   foreignKey: "profileImageId",
  //   as: "profileImage",
  // });
  // UserModel.belongsTo(MediaModel, {
  //   foreignKey: "profileImageId",
  //   as: "profileImage",
  // });

  // MediaModel.hasOne(TransactionModel, {
  //   foreignKey: "receiptId",
  //   as: "receipt",
  // });
  // TransactionModel.belongsTo(MediaModel, {
  //   foreignKey: "receiptId",
  //   as: "receipt",
  // });

  // TransactionModel.hasOne(TransactionModel, {
  //   foreignKey: "transactionId",
  // });
  // TransactionModel.belongsTo(TransactionModel, {
  //   foreignKey: "transactionId",
  // });

  // UserModel.hasMany(TransactionModel, {
  //   foreignKey: "userId",
  // });
  // TransactionModel.belongsTo(UserModel, {
  //   foreignKey: "userId",
  // });

  // MediaModel.hasOne(AdminBeneficiaryModel, {
  //   foreignKey: "qrImageId",
  //   as: "qrImage",
  // });
  // AdminBeneficiaryModel.belongsTo(MediaModel, {
  //   foreignKey: "qrImageId",
  //   as: "qrImage",
  // });
};